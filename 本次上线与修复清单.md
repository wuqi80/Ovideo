# Drama 上线与修复清单（2026-06-14 ~ 06-15）

> 服务器：GCP e2-medium（香港 asia-east2），Ubuntu 24.04
> 访问：https://drama.rongyansuanli.com  （账号 admin / admin123）
> 分支：`refactor/v2`（已设为远程默认分支）

---

## 一、上线部署（从零到可用）

| 步骤 | 结果 |
|------|------|
| GCP 虚拟机 | e2-medium 2vCPU/4GB + 100GB SSD，静态 IP `34.92.234.111` |
| 域名 | 阿里云 A 记录 `drama.rongyansuanli.com` → 34.92.234.111 |
| 部署 | 一键脚本 `server_setup.sh`：建库/迁移/前端构建/venv/Nginx/systemd |
| 服务托管 | `systemd drama.service`，开机自启，崩溃自动重启 |
| HTTPS | Let's Encrypt 证书，http→https 自动跳转，**自动续期**（certbot.timer）|
| 密钥 | 全部按本地注入 systemd；脱敏模板入库，真实密钥不进仓库 |

**运维命令**：`sudo systemctl {status\|restart} drama` / `sudo journalctl -u drama -f` / `tail -f ~/deploy/logs/backend.log`

---

## 二、修复的 Bug（按提交）

| 提交 | 模块 | 问题 → 修复 |
|------|------|------------|
| `a711fb4` | 视频 | **飞升卡 0%**：图 base64 数 MB，跨境传 ARK 30s 写超时 → 放大到 180s。实测大图出片 ✅ |
| `ca26053` | Worker | **死循环**：无 GPU agent 时 ComfyUI 任务被 lite worker 无限弹回刷屏 → 加弹回上限（约 60s）后判失败并给提示 |
| `ae2a64f` | 视频 | **飞升/渡劫必失败**：账号没开通 Seedance 2.0 → 改用已开通的 **1.0 Pro**（`doubao-seedance-1-0-pro-250528`），留 env 可切回 |
| `66eb729` | 配音 | **TTS 念出说话人名和引号**：`小悟：「别跟我说话」`整串被朗读 → 新增解析，只念引号内台词 |
| `2dad5eb` | 性能 | **加载慢**：出图等生成端点 `async def` 里用阻塞 `requests`，单进程下一次出图冻住整个事件循环 → 改 `asyncio.to_thread`；+ 启用 nginx gzip（JS 包 2.2MB→583KB）；+ 修音频混音路径（`services/` 多了一级导致 FileNotFoundError 500）|
| `a633103` | 用户 | **新用户建项目按钮无效（500）**：建用户漏传 user_id 生成 `user_xxx`，与全站「user_id==用户名」约定冲突触发外键冲突 → 3 处建用户路径统一 `user_id=username` |
| `2ca8ae1` | 视频 | **Sora2/Veo「无法下载图片」**：取图函数不认 file_id（前端就传 file_id）→ 加 FileDAO 回退；+ HappyHorse ratio `adaptive` 兜底 |
| `8afc8ad` | 配置 | `.gitignore` 密钥段加注释；requirements 放宽 uvicorn/websockets/httpx 版本冲突（Linux+Py3.12 装不上）|

---

## 三、视频模型可用性（实测）

| UI 名称 | 实际模型 | 状态 |
|---------|---------|------|
| 飞升 / 渡劫 | Seedance 1.0 Pro | ✅ 可用（已修型号 + 超时）|
| 大能 | Wan2.6（百炼）| ✅ 实测出片 |
| 合体 | Kling（百炼）| ✅ 实测出片 |
| 大乘 | Vidu（百炼）| ✅ 实测出片 |
| 炼虚 | HappyHorse（百炼）| ✅ 实测出片 |
| 金丹 | Sora2（laozhang）| ❌ 上游网关 503，等恢复 |
| 化神 | Veo（laozhang）| ❌ 上游网关 503，代码已修，等恢复 |
| 练气 / 各阶 | ComfyUI 本地模型 | ❌ 需接 GPU agent，本服务器没有 |

文本（DeepSeek/Gemini）、出图（Gemini/豆包）、配音（MiniMax）均已实测可用。

---

## 四、待你处理（非代码，账号/外部侧）

- [ ] **Sora2/Veo**：等 laozhang 网关 sora/veo 端点恢复（持续 503，非本项目问题）；若长期不行可换 API 网关
- [ ] **Seedance 2.0**：如需 2.0，去[火山方舟控制台](https://console.volcengine.com/ark)开通 `doubao-seedance-2-0-260128`，再设 env `SEEDANCE_MODEL_STANDARD/FAST` 切回
- [ ] **GPT Image 档位（天劫一/二阶）**：需在 laozhang 后台分组各建 token（默认出图走 Gemini/豆包，不影响主流程）

---

## 五、可选优化（建议，未做）

- **首帧图压缩**：视频上传前把图缩到几百 KB（现在几 MB），跨境上传更快更稳
- **多 uvicorn worker**：e2-medium 2 核可上 `--workers 2` 提并发（需先验证多进程下 lite worker 不重复消费）
- **前端代码分割**：主包 2.2MB（gzip 583KB），可 code-split 进一步提首屏

---

## 六、注意事项

- 改完前端需**强制刷新浏览器**（Ctrl+Shift+R）加载最新 dist
- 已有「错号用户」（user_id≠用户名）需在后台删除重建才能正常建项目；新建用户已自动正确
- 真实密钥只在服务器 systemd + 本地 gitignored 文件中，**仓库须保持私有**
