# Drama 全流程验证报告与上线待办

更新时间：2026-06-10
适用：`D:\Codex\Drama\deploy` 本地 Windows 部署（端口 6006）

---

## 一、全流程实测结论

补齐全部 API 密钥并启动 1 个 lite worker 后，对每个生成环节做了真实接口实测。**主流程每个环节都至少有一个供应商实测通过。**

| 环节 | 供应商 / 模型 | 结果 | 产物 |
| --- | --- | --- | --- |
| 文本 | DeepSeek（`/api/deepseek/chat`，SSE 流式） | ✅ | 完整中文文本 |
| 文本 | Gemini 文本（`/api/gemini/text`） | ✅ | 完整中文文本 |
| 图像 | Gemini 图像（`/api/gemini/image`，走 laozhang） | ✅ | webp 落盘 |
| 图像 | 豆包 SeedDream（`/api/materials/doubao`，ARK） | ✅ | 图片落盘 |
| 视频 | MiniMax Hailuo-02（图生，`minimax_i2v`） | ✅ | mp4 入库（图→视频）|
| 视频 | Kling（文生，`kling_t2v`，百炼） | ✅ | mp4 入库 |
| 配音 | Gemini TTS（`/api/audio/generate-speech`，官方端点） | ✅ | 可播放 wav |
| 配音 | MiniMax TTS（`/api/minimax/tts/sync`） | ✅ | mp3 入库 |

---

## 二、本次修复的代码缺陷（已提交 git `98de970` 及后续）

| 缺陷 | 文件 | 修法 |
| --- | --- | --- |
| TTS 用错模型 `gemini-2.0-flash`（不支持音频输出） | `audio_provider.py` | 改 `gemini-2.5-flash-preview-tts` |
| TTS 提示词 `[Voice:..]` 触发 "tried to generate text" | `audio_provider.py` | 改指令式 `Read ... aloud` |
| TTS 裸 PCM 直写 .wav 缺 RIFF 头、无法播放 | `audio_provider.py` | 用 `wave` 封头 |
| 缺 `google-genai` 依赖、`aiohttp` 过旧 | venv / `requirements.txt` | 安装 + 升级 + 补 requirements |
| MiniMax worker 硬编码 `720P`（Hailuo-02 仅支持 768P/1080P） | `worker.py` | 改 `768P` |
| `generate_sfx`/`generate_music` 用 TTS 模型必失败 | `audio_provider.py` | 改清晰报错，指向 MiniMax |
| `local_start.ps1` 启动后端拿不到环境变量（`& 脚本`里 `$env:` 不被 Start-Process 子进程继承）；端口未释放时跳过注入 | `local_start.ps1` | 改用 cmd `set` 注入 env；启动前先杀旧进程并轮询等端口释放 |
| 外部 API 视频任务无消费者（`LITE_WORKERS_COUNT=0`） | `local_start.ps1` | 改为 1，启动 lite worker |

---

## 三、上线后 / 有公网地址后 —— 待验证清单

以下供应商是**图生视频 / 参考图**类，代码会把输入图重新托管到 `cluster_url/uploads/...` 再交给外部网关。本地 `cluster_url` 是 `localhost`，外部网关抓不到，**本地无法验证**。上服务器或配好公网域名（`cluster_url` 指向公网）后逐一回测：

- [ ] **Sora2**（`sora2_i2v` / `sora2_morph`，laozhang）
- [ ] **Veo**（`veo_i2v` / `veo_morph`，laozhang）
  - ⚠️ 附带可疑点：`worker.py` 硬编码模型 `veo-3.1-landscape-fast-fl`，不在 laozhang 公布的模型列表（仅见 `veo-3.1-fast-generate-preview` / `veo-3.1-generate-preview`）。回测时若失败，优先核对/更换模型名（同 MiniMax 720P 类缺陷）。
- [ ] **Wan2.6**（`wan26_i2v`，百炼）
- [ ] **Vidu**（`vidu_r2v` / `vidu_morph`，百炼，参考图）
- [ ] **HappyHorse**（`happyhorse_r2v`，百炼，多参考图）

验证方式：传一张**公网可访问**的首帧图（或确保后端 `cluster_url` 为公网），提交对应 `task_type`，轮询 `/api/task/{id}` 至 `completed`，确认产出 mp4。

---

## 四、外部账号 / 配置待处理（非代码问题）

- [ ] **Seedance 视频开通**：火山方舟账号 `2101702750` 未激活 `doubao-seedance-2-0-260128` / `-fast-260128`。去[火山方舟控制台](https://console.volcengine.com/ark)开通模型服务即可（实测报错 `ModelNotOpen`）。
- [ ] **GPT Image 2 两把分组 token**：从 laozhang 后台「令牌」分别在**默认/VIP 分组**和 **Sora2Official 分组**各建一个 token：
  - `GPT_IMAGE_API_KEY`（卡片「laozhang GPT Image (天劫一阶/化神)」，模型 `gpt-image-2-vip`）
  - `SORA2_GPT_IMAGE_API_KEY`（卡片「laozhang Sora2 分组」，模型 `gpt-image-2` 官方）
  - 填入位置：`/admin` 后台 API 配置，或 `local_start.ps1` 环境变量；填后重启。
- [ ] **MiniMax 音乐生成**：`music-01` 模型需先上传参考曲（`refer_voice` + `refer_instrumental`，两步流程），纯歌词会报 `2013 invalid params`。如需纯文本生成，需评估是否改用更新的音乐模型（待测试确认）。

---

## 五、运行与重启（已修复为可靠）

```powershell
cd D:\Codex\Drama\deploy

# 启动（自动带全部 key + 1 个 lite worker；会先停旧进程再启）
powershell -ExecutionPolicy Bypass -File .\local_start.ps1

# 停止后端
powershell -ExecutionPolicy Bypass -File .\local_stop.ps1

# 停止后端 + 数据库/Redis 容器
powershell -ExecutionPolicy Bypass -File .\local_stop.ps1 -StopInfra
```

访问：`http://localhost:6006/projects`，账号 `admin / admin123`。

---

## 六、安全提醒

- 真实密钥在 `local_start.ps1` / `start_cluster.sh` / `new_html/.env` 中，均已 `.gitignore`，不进版本库。
- DB / 超管密码已从被跟踪的代码中脱敏。
- 仓库须保持私有。
