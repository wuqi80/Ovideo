# 2026-06-16 修复与功能清单

本日所有改动均已提交到分支 `refactor/v2` 并推送远端。下表按提交时间倒序，便于按问题追溯。
分支：`refactor/v2`

---

## 一、6 个问题排查结论（晚间集中修复）

| # | 问题 | 状态 | 根因 & 处理 | 相关提交 |
|---|------|------|------------|---------|
| 1 | 镜头重复 | ✅ 已修 | 合成查询 `LEFT JOIN files` 同一视频段可能多行 → 成片里同一镜头重复。按 `segment_id` 去重。 | `e154acd` |
| 2 | 分镜过多卡顿 | 🟡 已缓解 | ①分镜列表默认折叠20+展开；②结果视频懒加载（IntersectionObserver，进视口才 preload）；③自动保存防抖 2s→6s。根治（结果卡虚拟化 react-window）待评估。 | `09ef166` `07d149a` `e154acd` |
| 3 | 分镜更新未同步 | 🟢 基本覆盖 | 「同步最新分镜图」按钮（非破坏，保留已生成视频）+ 进视频页 `forceReloadSlices` 强制重载 + 占位会话自愈。已基于旧图生成的视频需手动重生成（合理）。 | `127ae54` `ff19887` |
| 4 | 视频不显示分镜图 | 🟡 修了主因 | 分镜批量入库漏 `generated_image_url`，"删旧建新"丢图 → 刷新空白。已让 create/batch_create 持久化画面URL+时长。剩：token 注入缺失/过期无兜底（待统一）。 | `31d5015` |
| 5 | 刷新后分镜丢失 | 🟡 修了主因 | 同 #4 的"删旧建新丢图"。已修持久化。剩：保存依赖触发/防抖，若未完成就刷新分镜项本身可能没入库——需改成"生成即入库 + upsert by item_id"根治。 | `31d5015` |
| 6 | 配音与视频不同步 | ✅ 已修 | 混音后没回写 `audio_duration_ms` → 合成读到 0 跳过音画对齐。已回写时长 + 合成兜底（有音频但 ms=0 时 ffprobe 实测）。*旧成片需重新合成生效。* | `e154acd` |

### 明天梳理时的关键提示
- #4/#5 的修复只防"未来"丢失；已经丢图的旧分镜需重新生成画面。
- #5 根治方向：分镜/画面生成完成时**立即写库**（不依赖 `generation-save-trigger`/防抖），并把「删全部+重建」改为 **upsert by item_id**，避免并发竞态删数据。`WorkspaceApp.tsx` 的 `saveEpisodeToBackend`。
- #2 根治：`VideoPage.tsx` 的 `sortedTaskGroups.map` 两处全量渲染是主热点，虚拟化收益最大。
- #4 token：图片 URL 注入 token 分散在 `secureMediaUrl`(VideoGenPage) / VideoPage 多处，建议抽统一工具 + 401 兜底。

---

## 二、合成成品 / 成品页

| 功能 | 说明 | 提交 |
|------|------|------|
| 一键合成成片（脚本） | ffmpeg 按镜对齐拼接视频+配音：视频短→定格补帧，配音短→补静音；归一化 1920×1080/30fps/h264+aac 后 concat。`deploy/scripts/compose_episode.sh`。关键修复：ffmpeg 加 `-nostdin` 防止吃掉 while 循环 stdin。 | `b7b213b` |
| 应用内「一键合成」 | `compose_service.py` 用 app 自身 DB 连接 + ffmpeg 后台异步任务；端点 `POST /api/episodes/{id}/compose` + `GET .../compose/status` 轮询；美化页加按钮。 | `b8efd34` |
| 多次合成全部保留 | 同一集多次合成各生成唯一 ID + 时间戳文件名/标题，普通 INSERT 不覆盖；成品页最新置顶+其余网格列出。 | `4fb9479` |
| 成品页挑选面板（逐镜选 take） | 「合成成品」按钮 → 弹面板：按分镜顺序列每镜所有视频 take 缩略图，逐镜点选一条（默认最新），确认后只用所选合成；新增 `GET /api/episodes/{id}/video-takes`，compose 支持 `selections{item_id:segment_id}`。分镜页加「导出到成品」入口。 | `484680b` |

---

## 三、视频页

| 修复 | 说明 | 提交 |
|------|------|------|
| 炼虚比例字段读错 | `submitDashScopeVideoTask` 误读通用 `params.ratio`，应读 `hh_ratio/hh_resolution/hh_duration` → 9:16 被忽略回落 16:9。已透传 hh_*。 | `25e6953` |
| 炼虚比例自动跟随分镜 | HappyHorse 无"自适应"档，默认写死 16:9 把竖图强转横版。改为按首张参考图实际宽高自动推断比例（用户手动改则不覆盖）。 | `77885f4` |
| 结果缩略图显示 | 多视频网格格子无固定高度 → 竖视频超大溢出。加 `aspect-video` + `object-contain`；`<video>` 加 `#t=0.1` 首帧封面（不再黑屏）。 | `c6682cf` |
| 结果视频懒加载 | `LazyVideo` 组件：IntersectionObserver，仅进视口附近才 preload，避免百级请求并发卡顿。 | `07d149a` |
| 同步最新分镜图 | 非破坏性：只更新变化的图片 URL + 参考图，保留任务组和已生成视频；顶栏黄标提示"N 个分镜图已更新"。 | `127ae54` |
| 全占位会话自愈 | 旧会话在分镜图生成前导入 → 全占位符，换设备/账号打开空卡。会话有任务组但 0 真实图、而分镜有图时自动重建。 | `ff19887` |

---

## 四、素材库 / 分镜 / 其它

| 改动 | 说明 | 提交 |
|------|------|------|
| 素材库兜底同步 | 各生成路径漏同步 → 素材库空。在统一保存入口 `save_generated_file_to_db` 兜底同步（幂等、非致命、按 episode 反查 project_id），落库即进库。 | `445d4bd` |
| 分镜列表折叠 | 默认显示前 20 个 + 按需展开，缓解镜头多时卡顿。 | `09ef166` |
| 分集卡片「复制」 | 派生副本并整套拷贝剧本内容。 | `d559e3f` |
| 分镜入库持久化画面 | create/batch_create 支持 `generated_image_url`+`planned_duration_ms`，防"删旧建新"丢图（见 #4/#5）。 | `31d5015` |

---

## 五、相关脚本/文件
- `deploy/scripts/compose_episode.sh` — 命令行一键合成成片（按镜对齐）
- `deploy/compose_service.py` — 应用内合成服务（异步任务 + take 选择 + 去重 + 音画对齐兜底）
- `deploy/scripts/backfill_media_library.sh` — 素材库一次性补全脚本
- `deploy/scripts/recover_wukong_2ep.sql` — 悟空项目素材恢复（历史）

> 注：本日修复多为"防未来再发生"，已损坏的历史数据（丢图、未对齐成片）需重新生成/合成才生效。
