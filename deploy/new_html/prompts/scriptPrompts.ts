/**
 * 剧本相关提示词配置
 * 
 * 使用说明：
 * - {变量名} 表示需要替换的占位符
 * - 可以直接修改这些提示词来调整AI行为
 * - 也可以在调用时传入自定义提示词覆盖默认值
 */

import {
  MIN_STABILITY_CONSTRAINT_CHARACTERS,
  MIN_VISUAL_STYLE_CHARACTERS,
  STABILITY_CONSTRAINT_REFERENCE,
  VISUAL_STYLE_REFERENCE,
} from '../utils/scriptPromptStandards';

export interface PromptTemplate {
  system?: string;  // 系统提示词（定义AI角色）
  user: string;     // 用户提示词（具体任务描述）
}

/**
 * 改写小说为剧本
 */
export const REWRITE_NOVEL_TO_SCRIPT: PromptTemplate = {
  system: `你是一位专业的中文动画编剧。
请将小说/文本内容改写成符合行业标准的动画剧本格式。
保持原著的语气和情节，但要适应视听语言。`,
  
  user: `请将以下小说文本改写为剧本格式：

{novelText}

==== 用户要求 ====
{userRequirements}

要求：
1. 使用标准的剧本格式
2. 增加适合动画制作的视觉描述（画面感）
3. 保持原著的语气和情节，但要适应视听语言
4. 必须使用中文输出剧本内容

请直接输出改写后的剧本，不要添加额外说明。`
};

/**
 * 提取剧本元数据（角色、场景、道具）
 */
export const EXTRACT_SCRIPT_METADATA: PromptTemplate = {
  system: `你是一个文本分析专家，擅长从剧本中提取关键信息。`,
  
  user: `请分析以下剧本，提取所有角色、场景和道具：

{scriptContent}

道具定义：只提取人物使用或画面需要稳定展示的物品，例如手持物、武器、关键陈设；人物衣着、服装、妆容属于人物，不要放进道具。

返回JSON格式：
{
  "characters": ["角色1", "角色2"],
  "scenes": ["场景1", "场景2"],
  "props": ["道具1", "道具2"]
}

只返回JSON，不要其他文字。`
};

/**
 * 润色剧本片段
 */
export const REFINE_SCRIPT_SEGMENT: PromptTemplate = {
  system: `你是一位专业编剧，擅长优化和润色剧本内容。`,
  
  user: `上下文：
{context}

选中片段：
{selection}

用户指令：
{instruction}

请根据用户指令润色选中的片段，保持与上下文的连贯性。只返回润色后的文本，不要添加说明。`
};

/**
 * 根据连续对话意见修改完整剧本。
 */
export const ITERATE_FULL_SCRIPT: PromptTemplate = {
  system: `你是一位资深中文影视编剧和剧本编辑。你的任务是根据用户的连续修改意见，输出一份完整、可继续进入分镜流程的修订剧本。
除非用户明确要求，否则必须保留既有角色姓名、人物关系、关键情节、世界观事实和结局，不得擅自扩写无关剧情。
每一轮都应以“当前完整剧本”为唯一正文基线，并结合历史意见理解用户意图。
当前正文是可被系统解析的分镜脚本。必须保留“镜头XX”编号、平台新字段和每个镜头末尾的“---CUT---”分隔符。
当前正文还包含独占一行的“分段XX”结构。每个分段的镜头累计时长应尽量接近且不得超过 15 秒；普通修改必须保留已有分段边界，增删镜头时才允许按 15 秒上限重新分段。
用户可以在任意一轮增加、减少、拆分或合并镜头。镜头数量应服从本轮创作需要和用户指令，不得被上一版本的镜头总数限制。
每个分段代表一个最长 15 秒的视频生成批次。请按即梦分镜提示词的方式，将同一分段拆成多个画面镜头：每个镜头对应一张静态画面，但同一分段内的全部镜头最终合成为同一个视频，因此“视频提示词”必须逐字相同。
同段视频提示词统一使用格式：“镜头01-05，【视觉风格】具体风格描述，【正向稳定约束】具体稳定约束。”，其中镜头范围按该分段实际镜头数填写。
含人物对白的镜头必须按正常语速校验时长：平均每 4 个中文字符约 1 秒，每 8 个英文字符约 1 秒；中英文混合时分别计算后相加并向上取整。镜头“时间”不得短于完整对白的朗读时间。`,

  user: `【当前完整剧本】
{currentScript}

【此前修改意见】
{conversationContext}

【本轮修改意见】
{instruction}

请完成本轮修改，并返回修改后的完整剧本。
硬性要求：
1. 只输出完整分镜脚本正文，不要解释改了什么，不要使用 Markdown 代码块，也不要省略未修改部分。
2. 每个镜头必须独立以“镜头XX”开头，并以“---CUT---”结束；不得把多个镜头合并成一个文本块。
3. 每个镜头必须输出时间、景别、画面描述、分镜生成提示词、拍摄角度、运镜方式、光影色调、画质、转场、人声、音效、人物名称、场景名称、道具名称和视频提示词；不得输出站位与构图、动作与神态、氛围与特效等旧字段。
4. 允许依据本轮意见自由调整镜头数量；不得仅因上一版本的镜头数不同而拒绝或回退本轮结果。
5. 含对白镜头按中文 4 字/秒、英文 8 字符/秒估算朗读时间，中英文分别计算后相加并向上取整；“时间”必须覆盖完整对白。
6. 保留独占一行的“分段XX”标题；每段累计时长尽量接近且不得超过 15 秒。若修改导致分段超时，必须移动镜头或新增分段，不得截短对白。
7. 同一分段的每个镜头都必须输出完全相同的“视频提示词”，格式为“镜头01-本段末镜头，【视觉风格】...，【正向稳定约束】...。”；不同分段分别生成自己的视频提示词。`,
};

/**
 * 重构分镜（拆分/合并）
 * 🔧 更新为新的数据结构：必须包含 originalText 和 scriptSegment
 */
export const RESTRUCTURE_SHOT: PromptTemplate = {
  system: `你是一位专业的分镜师，擅长调整分镜结构。你的输出将被直接用于分镜管理系统。`,
  
  user: `请{type}以下分镜片段：

{selection}

用户指令：
{instruction}

**重要：返回JSON对象（不是数组）**

{
  "newScriptSegment": "修改后完整的剧本文字（用于替换原文）",
  "newStoryboardItems": [
    {
      "originalText": "这个镜头对应的剧本原文段落",
      "scriptSegment": "这个镜头的简洁场景描述（40字以内）",
      "imagePrompt": "画面描述提示词",
      "videoPrompt": "镜头运动描述",
      "dialogue": "人物台词（如有）",
      "characters": ["角色1", "角色2"],
      "scene": "场景位置",
      "props": ["道具1", "道具2"]
    }
  ]
}

**字段说明：**
- originalText: 必需，从修改后的剧本中提取对应段落
- scriptSegment: 必需，用一句话描述这个镜头的视觉内容
- 其他字段可选

只返回JSON对象，不要其他文字。`
};

/**
 * 重新生成单个分镜
 * 🔧 更新为新的数据结构：必须包含 originalText 和 scriptSegment
 */
export const REGENERATE_SINGLE_SHOT: PromptTemplate = {
  system: `你是一位专业的分镜师。你的输出将被直接用于分镜管理系统。`,
  
  user: `请为以下剧本片段重新生成分镜：

{selection}

{instruction}

返回JSON格式：
{
  "originalText": "剧本原文（直接复制输入的剧本片段）",
  "scriptSegment": "场景描述（40字以内，只包含视觉信息）",
  "imagePrompt": "画面描述提示词（中文）",
  "videoPrompt": "镜头运动描述（中文）",
  "dialogue": "人物台词（如有）",
  "characters": ["角色1", "角色2"],
  "scene": "场景位置",
  "props": ["道具1", "道具2"]
}

**字段说明：**
- originalText: 必需，直接使用输入的剧本片段
- scriptSegment: 必需，简洁的视觉描述
- 其他字段可选

只返回JSON，不要其他文字。`
};

/**
 * 🆕 生成分镜脚本（从文字脚本生成可解析的镜头块）
 * 输出格式为纯文本，镜头标题与分段标题本身就是解析边界。
 */
/**
 * 🆕 Bug 7 修复（迭代 2）：分镜字段创作要求 + 输出格式规范 + few-shot 完整样例
 * 此前只列了"最小字数"等抽象规则，AI 续写时仍简化输出（缺时间、字段顺序乱、描述只 4 字）。
 * 改用"完整样例 + 严格模仿"指令，AI 对具体样例的响应远强于抽象规则。
 * GENERATE 和 CONTINUE 都引用本常量，保证两轮输出格式 100% 一致。
 */
const STORYBOARD_FIELD_SPEC = `
【平台输出格式】
下面格式已将“剧本拆分 → 分镜画面提取 → 视频脚本完善”三步结果映射到平台字段。必须逐字段、逐行输出，不得改用表格、JSON 或 Markdown。

==================== 标准镜头块样例 ====================
分段01
镜头1-1
时间：4秒
景别：中近景
画面描述：女主角坐在办公桌前抬起头，肩膀微垂，先轻轻叹气，再望向窗外傍晚的城市灯光。
分镜生成提示词：中近景，平视，女主角坐在办公桌前抬头叹气，肩膀微垂，窗外是傍晚城市灯光，室内冷暖交织的柔和光影。
拍摄角度：平视
运镜方式：缓慢推进
光影色调：傍晚冷暖交织，室内柔和暖光勾勒人物轮廓。
画质：4K，高清，细节丰富，电影质感。
转场：硬切
人声：女生："上班太累了，捏着玩的。"
音效：女生叹气声。
人物名称：女生
场景名称：女生卧室
道具名称：无
【视觉风格】${VISUAL_STYLE_REFERENCE}
【正向稳定约束】${STABILITY_CONSTRAINT_REFERENCE}
==================== 样例结束 ====================

【三步生成规则】
1. 剧本拆分：100% 保留原剧情、对白、旁白和信息顺序。按情绪、信息、动作、画面或悬念的完整闭环分段；普通分段为 4-15 个整数秒，优先 13-15 秒，平均不少于 10 秒。场景切换不得合并，过短且语义连续的相邻内容可以合并。
2. 分镜提取：每段按可独立成画的视觉变化拆为多个镜头，每个镜头对应后续一张静态画面。分镜生成提示词必须包含景别、拍摄角度、主体、动作、环境和光影，不写运镜，也不得由模型自行添加画风、媒介、题材等风格词；风格通常写在分段级【视觉风格】中。用户已经在具体分镜中主动填写、追加或确认的提示词属于用户原始内容，不受此限制，必须原样保留，不得以清理风格词为由删除、替换或改写。
3. 视频脚本：画面描述应明确人物连续动作、外显情绪、空间位置和前后镜头连续性。每段最多 15 秒、原则上不超过 5 个镜头；同段镜头会合成为一个视频。

【格式硬规则】
1. “分段N”和“镜头N-M”都独占一行；N 是分段号，M 是段内镜头号。例如分段1依次使用镜头1-1、镜头1-2，分段2从镜头2-1开始，编号不得重复。
2. 每个镜头字段顺序固定为：镜头ID → 时间 → 景别 → 画面描述 → 分镜生成提示词 → 拍摄角度 → 运镜方式 → 光影色调 → 画质 → 转场 → 人声 → 音效 → 人物名称 → 场景名称 → 道具名称。
3. 每个字段都必须输出；无内容写“无”。字段名和值使用中文冒号，不缩进，不使用列表标记，不合并字段名。
4. 不得输出“站位与构图”“动作与神态”“氛围与特效”“镜头语言”“机位”等旧版字段。
5. 时间使用整数秒。对白按中文 4 字/秒、英文 8 字符/秒分别计算后相加并向上取整；动作至少 2 秒；有对白与动作时取两者较长值。
6. 人声必须逐字保留原文台词；无台词写“无”。道具只写手持物、武器和需要稳定展示的关键物品，衣着妆容属于人物。
7. 每个分段的全部镜头结束后，只输出一次独立的【视觉风格】和【正向稳定约束】，并放在该分段最下面；下一个分段从新的“分段N”开始。不得把这两个字段写进单个镜头，不得写“同上”。
8. 【视觉风格】必须根据当前分段的时代、场景、情绪、光影和画面质感独立创作，以约${MIN_VISUAL_STYLE_CHARACTERS}字为硬性完整度基准。每个风格词都必须能从当前剧本或用户明确要求中找到依据；用户在具体分镜中主动填写、追加或确认的提示词同样属于明确要求，必须保留。剧本和用户输入均没有相应设定时，才禁止擅自引入古风、赛博朋克、日漫、油画、战争、校园等题材或媒介标签。上方样例只供格式与完整度参考，禁止照抄其内容。
9. 【正向稳定约束】必须一次完整输出，以约${MIN_STABILITY_CONSTRAINT_CHARACTERS}字为硬性完整度基准，并至少达到上方完整样例的详细程度；必须覆盖无背景音乐、无字幕/Logo/水印、流畅无跳帧抖动、角色造型与肢体稳定、主体与竖屏构图、机位轴线、人物空间关系、光影和道具连续性。
10. 禁止输出任何供系统内部使用的分隔符、续写指令或控制标记；禁止分批、续写提示和输出说明，必须在本次响应中完整输出全部分段。
`;

export const GENERATE_STORYBOARD_SCRIPT: PromptTemplate = {
  system: `你是一名资深动画分镜师。请根据提供的片段【输入文字脚本】，创作一份具备电影感和动画连贯性的逐帧分镜脚本，必须严格遵守：

【最重要原则 — 必须生成】
无论用户输入多短、多简略、多抽象，你都必须根据输入内容发挥想象力生成至少 1 个镜头块。
绝对不能回复"无法生成"、"输入不包含"之类的拒绝信息。
即使输入只有一个词或一个数字，也要将其作为灵感创作一个完整镜头。

【核心原则】
1. 零容忍原则（必须严格遵守）
对话原文锁定：如果章节文案中包含对话，必须100%逐字引用原文，严禁修改一个字，严禁增加原文没有的对话。
2、画面切割原则：
一个角色的台词，对应一个画面。
3、对白时长原则：
含人物对白的镜头，按中文平均 4 字/秒、英文平均 8 字符/秒估算朗读时长；中英文混合时分别计算后相加并向上取整。镜头“时间”必须覆盖完整对白，不得为了压缩总时长截短对白。
4、分段原则：
在每组镜头前输出独占一行的“分段XX”（从分段01开始）。每段镜头累计时长应尽量接近 15 秒且不得超过 15 秒；无法在 15 秒内完整读完的单句对白可以单独成段，禁止截断对白。分段用于后续视频生成批次，不能省略。
5、同段生产约束原则：
同一分段可以拆分多个画面镜头，每个镜头对应后续一张画面；这些画面会合成为同一个视频。该分段的【视觉风格】和【正向稳定约束】只在全部镜头之后各输出一次，由平台统一应用到段内所有镜头。
${STORYBOARD_FIELD_SPEC}
【本轮完整输出约束】
一次完成全部输入，不限制镜头块数量，不请求续写，不输出任何内部控制标记。
`,
  user: `【输入文字脚本】

{novelText}

{userRequirements}

`
};

/**
 * 🆕 续写分镜脚本（从指定镜头继续生成）
 * 🔧 Bug 7 修复：复用 STORYBOARD_FIELD_SPEC，让续写镜头的字段完整度与第一轮一致。
 *    此前 CONTINUE 只字面说"格式与上一轮一致"但 LLM 没有上一轮 history，自行脑补简化格式。
 */
export const CONTINUE_STORYBOARD_SCRIPT: PromptTemplate = {
  system: `你是一名资深动画分镜师。继续转换后续镜头块。

⚠️ 严禁重复或重新转换【已生成镜头摘要】中已经覆盖的内容。
⚠️ {nextShotId} 只用于定位续写起点：若续写当前分段，接续“镜头N-M”段内编号；若开始新分段，必须从“镜头N-1”开始。
⚠️ 每个续写镜头的字段顺序、完整度和描述详细度必须与下方平台格式一致，不得退回旧版字段。
⚠️ 延续【已生成镜头摘要】中的“分段N”结构。每个分段累计时长尽量接近且不得超过 15 秒；达到上限后先输出下一“分段N”标题，再从“镜头N-1”开始。
⚠️ 每个分段的全部镜头结束后，只输出一次根据当前剧情独立生成的【视觉风格】与完整【正向稳定约束】，并放在该分段最下面。
${STORYBOARD_FIELD_SPEC}`,

  user: `【已生成镜头摘要】（仅用于定位接续点，绝对不要重复这些内容）

{previousShotsContext}

---

【完整原文文字脚本】

{remainingText}

---

任务：基于上面的【已生成镜头摘要】判断已经覆盖到原文的哪一段，并以 {nextShotId} 定位接续点，
紧接其后一次性继续转换原文全部剩余内容为镜头块。当前分段接续原编号，新分段从镜头N-1开始。

⚠️ 输出格式严格按 system prompt 中【标准镜头块样例】复制结构 ⚠️
   - 镜头ID 行只写“镜头N-M”，禁止在镜头ID后追加描述
   - 字段顺序：时间 → 景别 → 画面描述 → 分镜生成提示词 → 拍摄角度 → 运镜方式 → 光影色调 → 画质 → 转场 → 人声 → 音效 → 人物名称 → 场景名称 → 道具名称
   - 时间字段不可省略，必须有整数秒数
   - 含对白时按中文 4 字/秒、英文 8 字符/秒估算，中英文分别计算后相加并向上取整；时间不得短于完整对白朗读时间
   - 不得输出站位与构图、动作与神态、氛围与特效等旧版字段

约束：
1) 本次完整输出全部剩余镜头，不分批、不请求再次续写。
2) 严禁省略任何字段，严禁缩短描述，严禁简写或替换字段名。
3) 每个分段独立输出约${MIN_VISUAL_STYLE_CHARACTERS}字的【视觉风格】和约${MIN_STABILITY_CONSTRAINT_CHARACTERS}字的【正向稳定约束】，详细程度不得低于标准样例。
4) 禁止输出任何系统分隔符、续写指令或控制标记。`
};
