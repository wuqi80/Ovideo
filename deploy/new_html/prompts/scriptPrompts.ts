/**
 * 剧本相关提示词配置
 * 
 * 使用说明：
 * - {变量名} 表示需要替换的占位符
 * - 可以直接修改这些提示词来调整AI行为
 * - 也可以在调用时传入自定义提示词覆盖默认值
 */

export interface PromptTemplate {
  system?: string;  // 系统提示词（定义AI角色）
  user: string;     // 用户提示词（具体任务描述）
}

/**
 * 改写小说为剧本
 */
export const REWRITE_NOVEL_TO_SCRIPT: PromptTemplate = {
  system: `你是一位专业的中文动画编剧。
请将小说/文本内容改写成符合行业标准的动画剧本格式。
保持原著的语气和情节，但要适应视听语言。`,
  
  user: `请将以下小说文本改写为剧本格式：

{novelText}

==== 用户要求 ====
{userRequirements}

要求：
1. 使用标准的剧本格式
2. 增加适合动画制作的视觉描述（画面感）
3. 保持原著的语气和情节，但要适应视听语言
4. 必须使用中文输出剧本内容

请直接输出改写后的剧本，不要添加额外说明。`
};

/**
 * 提取剧本元数据（角色、场景、道具）
 */
export const EXTRACT_SCRIPT_METADATA: PromptTemplate = {
  system: `你是一个文本分析专家，擅长从剧本中提取关键信息。`,
  
  user: `请分析以下剧本，提取所有角色、场景和道具：

{scriptContent}

道具定义：只提取人物使用或画面需要稳定展示的物品，例如手持物、武器、关键陈设；人物衣着、服装、妆容属于人物，不要放进道具。

返回JSON格式：
{
  "characters": ["角色1", "角色2"],
  "scenes": ["场景1", "场景2"],
  "props": ["道具1", "道具2"]
}

只返回JSON，不要其他文字。`
};

/**
 * 润色剧本片段
 */
export const REFINE_SCRIPT_SEGMENT: PromptTemplate = {
  system: `你是一位专业编剧，擅长优化和润色剧本内容。`,
  
  user: `上下文：
{context}

选中片段：
{selection}

用户指令：
{instruction}

请根据用户指令润色选中的片段，保持与上下文的连贯性。只返回润色后的文本，不要添加说明。`
};

/**
 * 根据连续对话意见修改完整剧本。
 */
export const ITERATE_FULL_SCRIPT: PromptTemplate = {
  system: `你是一位资深中文影视编剧和剧本编辑。你的任务是根据用户的连续修改意见，输出一份完整、可继续进入分镜流程的修订剧本。
除非用户明确要求，否则必须保留既有角色姓名、人物关系、关键情节、世界观事实和结局，不得擅自扩写无关剧情。
每一轮都应以“当前完整剧本”为唯一正文基线，并结合历史意见理解用户意图。
当前正文是可被系统解析的分镜脚本。必须保留“镜头XX”编号、完整字段和每个镜头末尾的“---CUT---”分隔符。
当前正文还包含独占一行的“分段XX”结构。每个分段的镜头累计时长应尽量接近且不得超过 15 秒；普通修改必须保留已有分段边界，增删镜头时才允许按 15 秒上限重新分段。
除非用户明确要求调整镜头数量，否则镜头总数和镜头编号必须与当前正文完全一致。用户只说“减少几个镜头”时，只允许合并少量镜头，保留至少 70% 的原镜头；用户明确指定目标数量时必须严格遵循目标数量。
含人物对白的镜头必须按正常语速校验时长：平均每 4 个中文字符约 1 秒，每 8 个英文字符约 1 秒；中英文混合时分别计算后相加并向上取整。镜头“时间”不得短于完整对白的朗读时间。`,

  user: `【当前完整剧本】
{currentScript}

【此前修改意见】
{conversationContext}

【本轮修改意见】
{instruction}

请完成本轮修改，并返回修改后的完整剧本。
硬性要求：
1. 只输出完整分镜脚本正文，不要解释改了什么，不要使用 Markdown 代码块，也不要省略未修改部分。
2. 每个镜头必须独立以“镜头XX”开头，并以“---CUT---”结束；不得把多个镜头合并成一个文本块。
3. 每个镜头保留镜头语言、画面描述、转场、人声、时间、音效、人物名称、场景名称和道具名称等完整字段。
4. 若本轮没有要求增删镜头，镜头总数必须保持不变；若要求“减少几个”，不得大幅删减剧情或镜头。
5. 含对白镜头按中文 4 字/秒、英文 8 字符/秒估算朗读时间，中英文分别计算后相加并向上取整；“时间”必须覆盖完整对白。
6. 保留独占一行的“分段XX”标题；每段累计时长尽量接近且不得超过 15 秒。若修改导致分段超时，必须移动镜头或新增分段，不得截短对白。`,
};

/**
 * 重构分镜（拆分/合并）
 * 🔧 更新为新的数据结构：必须包含 originalText 和 scriptSegment
 */
export const RESTRUCTURE_SHOT: PromptTemplate = {
  system: `你是一位专业的分镜师，擅长调整分镜结构。你的输出将被直接用于分镜管理系统。`,
  
  user: `请{type}以下分镜片段：

{selection}

用户指令：
{instruction}

**重要：返回JSON对象（不是数组）**

{
  "newScriptSegment": "修改后完整的剧本文字（用于替换原文）",
  "newStoryboardItems": [
    {
      "originalText": "这个镜头对应的剧本原文段落",
      "scriptSegment": "这个镜头的简洁场景描述（40字以内）",
      "imagePrompt": "画面描述提示词",
      "videoPrompt": "镜头运动描述",
      "dialogue": "人物台词（如有）",
      "characters": ["角色1", "角色2"],
      "scene": "场景位置",
      "props": ["道具1", "道具2"]
    }
  ]
}

**字段说明：**
- originalText: 必需，从修改后的剧本中提取对应段落
- scriptSegment: 必需，用一句话描述这个镜头的视觉内容
- 其他字段可选

只返回JSON对象，不要其他文字。`
};

/**
 * 重新生成单个分镜
 * 🔧 更新为新的数据结构：必须包含 originalText 和 scriptSegment
 */
export const REGENERATE_SINGLE_SHOT: PromptTemplate = {
  system: `你是一位专业的分镜师。你的输出将被直接用于分镜管理系统。`,
  
  user: `请为以下剧本片段重新生成分镜：

{selection}

{instruction}

返回JSON格式：
{
  "originalText": "剧本原文（直接复制输入的剧本片段）",
  "scriptSegment": "场景描述（40字以内，只包含视觉信息）",
  "imagePrompt": "画面描述提示词（中文）",
  "videoPrompt": "镜头运动描述（中文）",
  "dialogue": "人物台词（如有）",
  "characters": ["角色1", "角色2"],
  "scene": "场景位置",
  "props": ["道具1", "道具2"]
}

**字段说明：**
- originalText: 必需，直接使用输入的剧本片段
- scriptSegment: 必需，简洁的视觉描述
- 其他字段可选

只返回JSON，不要其他文字。`
};

/**
 * 🆕 生成分镜脚本（从文字脚本生成可解析的镜头块）
 * 输出格式为纯文本，使用 ---CUT--- 分隔镜头块
 * 🔧 严格按照用户输入的镜头数量，不创造新镜头
 */
/**
 * 🆕 Bug 7 修复（迭代 2）：分镜字段创作要求 + 输出格式规范 + few-shot 完整样例
 * 此前只列了"最小字数"等抽象规则，AI 续写时仍简化输出（缺时间、字段顺序乱、描述只 4 字）。
 * 改用"完整样例 + 严格模仿"指令，AI 对具体样例的响应远强于抽象规则。
 * GENERATE 和 CONTINUE 都引用本常量，保证两轮输出格式 100% 一致。
 */
const STORYBOARD_FIELD_SPEC = `
【输出格式 — 必须严格按以下样例逐字段、逐行模仿，不允许省略任何字段，不允许调整字段顺序，不允许改变缩进】

==================== 标准镜头块样例 ====================
镜头10
镜头语言：
    取景：中近景
    摄像机角度：平视
    镜头运动：固定
    机位：女主角正前方
画面描述：
    站位与构图：女主角抬起头，表情有些疲惫和无奈。
    动作与神态：女生叹了口气，语气中带着上班族的辛酸。
    氛围与特效：背景中，窗外是傍晚的景色，暗示下班后的疲惫感。
转场：硬切
人声：女生："上班太累了，捏着玩的。"
时间：2秒
音效：女生叹气声。
人物名称：女生
场景名称：女生卧室
道具名称：无
---CUT---
==================== 样例结束 ====================

【格式硬规则 — 每条违例都将导致下游解析失败】

1. **镜头ID 行**：只写 "镜头XX"（如 "镜头11"），独占一行，**绝对不要在镜头ID 行后面加任何描述文字**（错误示例：✗"镜头11：中近景，平视。女主角站在画面中央..."）
2. **必须输出两个分组标题**：
   - "镜头语言：" 独占一行（无缩进）
   - "画面描述：" 独占一行（无缩进）
3. **缩进规则**：
   - 在"镜头语言："下方的 4 个字段（取景/摄像机角度/镜头运动/机位）每行必须以 **4 个空格** 开头
   - 在"画面描述："下方的 3 个字段（站位与构图/动作与神态/氛围与特效）每行必须以 **4 个空格** 开头
   - 其他字段（转场/人声/时间/音效/人物名称/场景名称/道具名称）行首**不缩进**
4. **字段顺序严格按样例**：镜头ID → 镜头语言（含 4 子字段）→ 画面描述（含 3 子字段）→ 转场 → 人声 → 时间 → 音效 → 人物名称 → 场景名称 → 道具名称 → ---CUT---
5. **每个字段都不能省略**（即使内容是"无"也要输出该字段并写"无"）—— 特别注意时间字段绝不可丢
6. 字段名严格使用上述名称：用"摄像机角度"不用"角度"，用"镜头运动"不用"运动"
7. 字段值与字段名之间用**中文冒号"："**（不是英文 ":"），冒号后无空格直接跟值
8. 行首**禁止**使用列表标记（- * • 等）
9. **禁止**合并字段名（错误示例：✗"取景/角度/机位：近景"）—— 即使用户输入采用了合并格式，输出也必须拆开
10. **道具名称定义**：只写人物使用或画面需要稳定展示的物品（手持物、武器、关键陈设等）；服装、衣着、妆容属于人物，不要写入道具名称；如无写"无"

【描述详细度要求 — 每个字段最少字数】

- 取景：至少 2 字（如"中景"、"近景"、"中近景"）
- 摄像机角度：至少 2 字
- 镜头运动：至少 2 字
- 机位：至少 4 字（要有空间锚点，如"女主角正前方"、"侧面 30 度"）
- 站位与构图：至少 15 字（具体描述人物位置 + 表情/姿态线索）
- 动作与神态：至少 20 字（具体的肢体动作 + 面部表情 + 情绪状态）
- 氛围与特效：至少 12 字（描述光影/天气/环境颗粒/特效，无特效时也要写至少 12 字的环境描述）
- 转场：至少 2 字（如"硬切"、"叠化"、"快摇"）
- 人声：原文台词原样引用，无台词写"无"
- 时间：必须有秒数（如"2秒"、"3.5秒"）；含对白时，按中文平均 4 字/秒、英文平均 8 字符/秒估算，中英文分别计算后相加并向上取整，镜头时间不得短于完整对白朗读时间
- 音效：至少 2 字描述（无音效写"无"）

⚠️ **续写场景特别强调**：续写镜头（镜头11+）的每个字段完整度、缩进、分组标题、字段顺序、描述详细度都必须与样例（即上一轮的镜头01-10）100% 一致。绝不允许变简、缺字段、缺分组标题、缺缩进。
`;

export const GENERATE_STORYBOARD_SCRIPT: PromptTemplate = {
  system: `你是一名资深动画分镜师。请根据提供的片段【输入文字脚本】，创作一份具备电影感和动画连贯性的逐帧分镜脚本，必须严格遵守：

【最重要原则 — 必须生成】
无论用户输入多短、多简略、多抽象，你都必须根据输入内容发挥想象力生成至少 1 个镜头块。
绝对不能回复"无法生成"、"输入不包含"之类的拒绝信息。
即使输入只有一个词或一个数字，也要将其作为灵感创作一个完整镜头。

【核心原则】
1. 零容忍原则（必须严格遵守）
对话原文锁定：如果章节文案中包含对话，必须100%逐字引用原文，严禁修改一个字，严禁增加原文没有的对话。
2、画面切割原则：
一个角色的台词，对应一个画面。
3、对白时长原则：
含人物对白的镜头，按中文平均 4 字/秒、英文平均 8 字符/秒估算朗读时长；中英文混合时分别计算后相加并向上取整。镜头“时间”必须覆盖完整对白，不得为了压缩总时长截短对白。
4、分段原则：
在每组镜头前输出独占一行的“分段XX”（从分段01开始）。每段镜头累计时长应尽量接近 15 秒且不得超过 15 秒；无法在 15 秒内完整读完的单句对白可以单独成段，禁止截断对白。分段用于后续视频生成批次，不能省略。
${STORYBOARD_FIELD_SPEC}
【本轮长度约束】
若用户输入较长，本次最多输出10个镜头块；未覆盖完则最后一行输出：<<<CONTINUE_FROM 镜头XX>>>
若已全部转换完成，不要输出<<<CONTINUE_FROM>>>。
`,
  user: `【输入文字脚本】

{novelText}

{userRequirements}

`
};

/**
 * 🆕 续写分镜脚本（从指定镜头继续生成）
 * 🔧 Bug 7 修复：复用 STORYBOARD_FIELD_SPEC，让续写镜头的字段完整度与第一轮一致。
 *    此前 CONTINUE 只字面说"格式与上一轮一致"但 LLM 没有上一轮 history，自行脑补简化格式。
 */
export const CONTINUE_STORYBOARD_SCRIPT: PromptTemplate = {
  system: `你是一名资深动画分镜师。继续转换后续镜头块。

⚠️ 严禁重复或重新转换【已生成镜头摘要】中已经覆盖的内容。
⚠️ 必须从 {nextShotId} 开始编号，紧接【已生成镜头摘要】之后的原文部分继续转换。
⚠️ **每个续写镜头的格式（分组标题、缩进、字段顺序、字段完整度、描述详细度）必须与下方【标准镜头块样例】中的镜头10 100% 一致**。续写镜头不允许有任何简化（不允许跳过字段、不允许缩短描述、不允许丢分组标题或缩进）。
⚠️ 延续【已生成镜头摘要】中的“分段XX”结构。每个分段累计时长尽量接近且不得超过 15 秒；达到上限后先输出下一“分段XX”标题，再继续输出镜头。镜头编号仍按全文唯一序号接续，系统会在界面按段内顺序显示。
${STORYBOARD_FIELD_SPEC}`,

  user: `【已生成镜头摘要】（仅用于定位接续点，绝对不要重复这些内容）

{previousShotsContext}

---

【完整原文文字脚本】

{remainingText}

---

任务：基于上面的【已生成镜头摘要】判断已经覆盖到原文的哪一段，然后从 {nextShotId} 开始编号，
紧接其后继续转换原文剩余部分为镜头块。

⚠️ 输出格式严格按 system prompt 中【标准镜头块样例】的 镜头10 复制结构 ⚠️
   - 镜头ID 行只写 "镜头XX"，**禁止**在镜头ID 后追加任何描述文字
   - 必须输出 "镜头语言：" 和 "画面描述：" 两个分组标题（独占一行，无缩进）
   - 镜头语言下的 4 个子字段、画面描述下的 3 个子字段必须以 4 空格缩进开头
   - 字段顺序：镜头语言（取景/摄像机角度/镜头运动/机位）→ 画面描述（站位与构图/动作与神态/氛围与特效）→ 转场 → 人声 → 时间 → 音效 → 人物名称 → 场景名称 → 道具名称
   - **时间字段不可省略**，必须有秒数
   - 含对白时按中文 4 字/秒、英文 8 字符/秒估算，中英文分别计算后相加并向上取整；时间不得短于完整对白朗读时间
   - 站位与构图至少 15 字，动作与神态至少 20 字，氛围与特效至少 12 字

约束：
1) 本次最多输出 10 个镜头块。
2) 若原文还有未覆盖的内容，末尾输出 <<<CONTINUE_FROM 镜头XX>>>（XX 为下一镜头编号）。
3) 若已经把原文全部转换完成，不要输出 <<<CONTINUE_FROM>>>。
4) 严禁省略任何字段，严禁缩短描述，严禁简写字段名。`
};

