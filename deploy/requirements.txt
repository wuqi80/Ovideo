# === Web Framework ===
fastapi==0.109.0
# 2026-06-14：原 ==0.27.0 硬锁在 Ubuntu 24.04 + Python 3.12 下与 httpx/websockets
# 解析冲突（ResolutionImpossible），放宽为 >=0.27 让 pip 自解，GCP 部署实测通过。
uvicorn[standard]>=0.27
python-multipart==0.0.9
pydantic==2.5.3
pydantic-settings==2.1.0

# === HTTP / WebSocket ===
aiohttp==3.9.1
requests==2.31.0
# 2026-06-14：同上，原 ==12.0 放宽为 >=12（见 uvicorn 注释）。
websockets>=12
python-socketio==5.11.0

# === Database ===
asyncpg==0.29.0
redis[hiredis]==5.0.1

# === File / Media ===
aiofiles==23.2.1
Pillow==10.2.0
ffmpeg-python==0.2.0

# === Auth / Security ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
PyJWT==2.8.0
bcrypt==4.1.2

# === AI / LLM ===
openai>=1.35.0

# === Utils ===
python-dateutil==2.8.2
python-dotenv==1.0.0
pyyaml>=6.0

# === Testing ===
pytest>=8.0
pytest-asyncio>=0.23
# Tests use ASGITransport directly instead of Starlette's legacy TestClient.
httpx>=0.28.1,<1

# === Optional: MinIO storage ===
# minio>=7.1.0
